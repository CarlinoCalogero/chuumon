export type Table = {
  tableNumber: number,
  numero_sala: number,
  numberOfMergedTables: number,
  top: number,
  left: number,
  rotate: number,
  ora: Date | null,
  nome_prenotazione: string | null,
  numero_persone: number | null,
  note: string | null
}
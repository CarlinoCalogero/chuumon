import { SQUARE_TABLE_EDGE_DIMENSION_IN_PIXELS } from '@/lib/utils';
import styles from './Tables.module.css'

interface OrderSnippetProps {

}

export function OrderSnippet({ }: OrderSnippetProps) {

    return (
        <div>

        </div>
    )
}